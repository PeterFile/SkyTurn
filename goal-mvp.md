Status: archived MVP goal prompt. This file records an early delivery target, not the current capability map or next active work item. For current code facts and remaining work, use `README.md`, `docs/natural-workflow-design.md`, and implementation paths under `apps/desktop/electron`, `packages/workflow-kernel`, `packages/persistence`, `packages/agent-bridge`, and `packages/ui-canvas`.

/goal Deliver a working end-to-end MVP of the workflow-platform orchestration loop: when a user enters a requirement in the workflow input box, the request is sent to Hermes-agent for decomposition and planning; Hermes-agent can call explicit workflow-card tools to create, update, and delete node cards; code-related task cards are dispatched to Codex; and the xyflow canvas visibly reflects the Hermes/Codex execution lifecycle through card creation, card mutation, task status changes, and task result/error updates.

Verify completion with a runnable local vertical-slice scenario: start the app using the existing project commands, submit one realistic software-development requirement from the input box, observe Hermes-agent planning multiple task cards, confirm the card create/update/delete tool path is actually used, dispatch at least one code task to Codex, and confirm the corresponding card records Codex execution state, result summary, error/blocker if any, and any relevant output reference such as changed files, branch/worktree, command output, or task artifact.

Preserve the existing architecture and product direction. Use the existing repo structure, package manager, xyflow canvas implementation, Hermes-agent entrypoints, Codex CLI/API integration points, agent bridge/runtime contracts, and existing IPC boundaries where present. If the project contains Electron main / renderer separation, local command execution must stay in the main/agent-bridge layer and the renderer must not directly spawn Hermes, Codex, shell commands, or file-system mutations. xyflow should continue to own node position, drag, zoom, and edge behavior. GSAP, if already present, may only be used for node-internal status motion and edge-flow polish, not for canvas coordinate control.

Implement the smallest shippable vertical slice, not a full workflow engine. Prefer minimal adapters, typed contracts, and narrow integration points over broad rewrites. Do not introduce unrelated auth, billing, cloud queues, multi-tenant permissions, database migrations, large UI redesigns, global refactors, or speculative agent frameworks unless the current codebase already requires them for the vertical slice. Keep the current node-card visual direction intact.

For card tools, inspect existing conventions first. If no convention exists, introduce a minimal Hermes-callable tool surface equivalent to: createWorkflowCard, updateWorkflowCard, and deleteWorkflowCard. The tools must validate input, produce deterministic card IDs or return created IDs, update canvas/store state, preserve enough metadata to trace the originating Hermes task, and support task lifecycle fields such as pending/planned, running, completed, failed, blocked, and retrying if those states already exist. Do not fake tool success: card mutations must be observable in the application state and UI.

For Codex dispatch, inspect the existing runtime model first. If Codex execution is already supported, wire code-task cards to the real dispatch path. If only discovery/mock execution exists, keep the mock path available for UI smoke tests but do not mark the Goal complete until the real Codex path is either connected or blocked with precise evidence. A code task should flow from Hermes plan -> card -> Codex dispatch -> execution update -> card result/error reflection.

Between iterations, inspect the latest evidence, make the smallest change that advances the end-to-end path, run the most relevant verification command available for touched packages, and record: files changed, commands run, what passed, what failed, what the current end-to-end gap is, and the next best action. Completion requires an auditable local run or test/demo script proving the full input -> Hermes-agent -> card tools -> Codex dispatch -> card reflection loop.

If Hermes-agent or Codex cannot be invoked because of missing binaries, credentials, local services, undocumented APIs, unavailable IPC contracts, or environment configuration, stop substantive work and report the exact attempted commands, files inspected, failure output, the blocker, the minimum missing input needed, and the smallest remaining patch plan. Do not declare the Goal complete on mock-only behavior, screenshots, assumptions, or unverified code paths.
