{
  "name": "skyturn",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "dev": "turbo run dev --filter=@skyturn/desktop",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "flow-kernel:acceptance": "turbo run flow-kernel:acceptance --filter=@skyturn/workflow-kernel --filter=@skyturn/persistence"
  },
  "devDependencies": {
    "turbo": "^2.9.18"
  }
}
